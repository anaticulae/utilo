# PR 1.9.0

## Pages

* [x] change default behavior of pages selector to return default value
      instead of throwing KeyError.

* [x] rename page selector parameter

## Feature

* [ ] fix panellizing workplan steps: see sections multi step example

* [x] fix printing correct working step. In current state, program name is
      printed.

## File

* [ ] add method 'make_package' to convert file path to python package path

## Test

* [x] add method 'utila.raw()' to hide:

    ```[python]
    assert len(abc)>2, print(abc)
    ```

    ```[python]
    assert len(abc)>2, utila.log_raw(abc)
    ```

## Utils

* [x] add context manager to chdir and change back after

    ```[python]
    width utila.chdir(path):
        execute..
    ```
